[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    --tb=short
    -v
    --ignore=tests/test_e2e_playwright.py
    --cov=app
    --cov-report=term-missing
    --cov-report=html:doc/coverage
